<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xfrog.platform.infrastructure.base.mapper.LangLocalMapper">
    <select id="queryByLangCorpusId" resultType="com.xfrog.platform.application.base.dto.LangLocalDTO">
        SELECT
            a.lang_code as langCode,
            a.local_value as langValue
        FROM
        lang_locales a
        JOIN langs b ON a.lang_id = b.id
            AND b.enabled = 1
            AND b.deleted = 0
        <where>
            a.deleted = false
            AND a.lang_corpus_id = #{langCorpusId}
        </where>
    </select>
</mapper>