<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xfrog.platform.infrastructure.base.mapper.LangMapper">
    <select id="queryAll" resultType="com.xfrog.platform.application.base.dto.LangDTO">
        SELECT
          *
        FROM
        langs l
        <where>
            l.deleted = false
            <if test="queryDTO.application != null and queryDTO.application != ''">
                AND l.application = #{queryDTO.application}
            </if>
        </where>
    </select>
</mapper>